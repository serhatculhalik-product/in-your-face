import Foundation

enum AppIdentity {
    static let displayName = "Meeting Incoming"
    static let descriptor = "Full-screen calendar alerts for Mac."
    static let onboardingWindowTitle = "Set Up Meeting Incoming"
}
